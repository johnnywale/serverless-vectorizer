# embed/Dockerfile.variant
ARG BASE_IMAGE
FROM ${BASE_IMAGE}

ARG VARIANT
ARG MODEL_TYPE

ENV MODEL_VARIANT=${VARIANT}
ENV MODEL_TYPE=${MODEL_TYPE}

RUN cd ${LAMBDA_TASK_ROOT}/ && ${LAMBDA_RUNTIME_DIR}/preload ${MODEL_TYPE}

CMD [ "bootstrap" ]